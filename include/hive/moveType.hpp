#pragma once

namespace Hive {
    /**
     * @brief This class defines a different types of moves.
     * 
     */
    enum class MoveType {
        DeployMove,
        DragMove,
        PassMove,
    };
}