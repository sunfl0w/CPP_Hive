#pragma once

namespace Hive {
    /**
     * @brief Thia class defines a different types of moves.
     * 
     */
    enum class MoveType {
        DeployMove,
        DragMove,
        PassMove,
    };
}