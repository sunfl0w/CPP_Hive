#include "SlidePath.hpp"

#include "Board.hpp"

namespace Hive{
    SlidePath::SlidePath() {

    }

    bool SlidePath::IsValidForPiece(const AxialPosition& piecePosition, const Board& board) const {
        std::vector<AxialPosition>
    }
}